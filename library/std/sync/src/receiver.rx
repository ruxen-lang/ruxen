class Receiver[T: Send]
  include Send

  lib "runtime/channel.c"
    def recv_raw as "ruxen_channel_recv"(self) -> T
    def try_recv_raw as "ruxen_channel_try_recv"(self) -> T
    def drop as "ruxen_channel_receiver_drop"(self) -> ()
  end
end
