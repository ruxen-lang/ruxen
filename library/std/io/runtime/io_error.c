#include "../../core/runtime/runtime.h"

/* Generated by Phase B of #06.75: carved from runtime.c.
   DO NOT add to this file unless you also update the top-level runtime.c #include order. */

/* ── IoError (tagged enum, Phase 2 #06.5) ─────────────────────────────
 *
 * Wire format (matches the Ruxen type-level `enum IoError` registered
 * in resolve/mod.rs — keep these constants in sync):
 *   offset 0: int32 tag
 *   offset 4: 4 bytes padding (payload aligns to 8)
 *   offset 8: variant-specific payload slot (one int64; pointer for
 *             `Other`, unused for unit variants)
 *
 * We allocate a uniform 16 bytes for every variant so the int64 slot
 * in `Result::Err` always points at a layout-stable struct.
 *
 * Variant tag values mirror the order in `resolve::register_builtins`:
 *   0  NotFound
 *   1  PermissionDenied
 *   2  AlreadyExists
 *   3  Interrupted
 *   4  WouldBlock
 *   5  InvalidInput
 *   6  UnexpectedEof
 *   7  BrokenPipe
 *   8  Other(message: String)
 *
 * Phase 2 #06.5 T1 — appended 11 message-carrying variants (idx 9..19)
 * to widen the classifier's coverage of POSIX errno without renumbering
 * the existing tags (callers may match on the numeric tag via
 * `IoError.kind()` and we must not invalidate those matches):
 *   9  ConnectionRefused(message: String)
 *  10  ConnectionReset(message: String)
 *  11  ConnectionAborted(message: String)
 *  12  NotConnected(message: String)
 *  13  AddrInUse(message: String)
 *  14  AddrNotAvailable(message: String)
 *  15  InvalidData(message: String)
 *  16  TimedOut(message: String)
 *  17  WriteZero(message: String)
 *  18  Unsupported(message: String)
 *  19  OutOfMemory(message: String)
 *
 * `InvalidData` and `WriteZero` have no portable errno mapping; they
 * exist for direct construction (parsers, serializers, short-write
 * detection in user code). The other nine are produced by
 * `ruxen_io_error_classify_errno` from real errno values.
 */
/* RUXEN_IO_ERROR_* tag macros now live in runtime.h so cross-package
 * callers (fs, env, net, rand, process, …) can reference them. */

void *ruxen_io_error_unit(int32_t tag) {
    int64_t *err = (int64_t *)ruxen_alloc(16);
    *(int32_t *)err = tag;
    err[1] = 0;
    return err;
}

static void *ruxen_io_error_other(const char *message) {
    int64_t *err = (int64_t *)ruxen_alloc(16);
    *(int32_t *)err = RUXEN_IO_ERROR_OTHER;
    err[1] = (int64_t)ruxen_string_from(message ? message : "io error");
    return err;
}

/* Phase 2 #06.5 T1: allocator for the 11 new message-carrying variants
 * (ConnectionRefused, …, OutOfMemory). Layout is identical to `Other`
 * — int32 tag, 4 pad, char* payload — only the tag varies. Used by
 * the errno classifier path. User-side direct construction goes
 * through the synthesized enum constructor in MIR lowering, which
 * lays out the same 16-byte struct. */
void *ruxen_io_error_struct(int32_t tag, const char *message) {
    int64_t *err = (int64_t *)ruxen_alloc(16);
    *(int32_t *)err = tag;
    err[1] = (int64_t)ruxen_string_from(message ? message : "io error");
    return err;
}

static int32_t ruxen_io_error_classify_errno(int saved_errno) {
    switch (saved_errno) {
        case ENOENT:        return RUXEN_IO_ERROR_NOT_FOUND;
        case EACCES:
        case EPERM:         return RUXEN_IO_ERROR_PERMISSION_DENIED;
        case EEXIST:        return RUXEN_IO_ERROR_ALREADY_EXISTS;
        case EINTR:         return RUXEN_IO_ERROR_INTERRUPTED;
#ifdef EAGAIN
        case EAGAIN:        return RUXEN_IO_ERROR_WOULD_BLOCK;
#endif
        case EINVAL:        return RUXEN_IO_ERROR_INVALID_INPUT;
        case EPIPE:         return RUXEN_IO_ERROR_BROKEN_PIPE;
        /* Phase 2 #06.5 T1 — network + resource-exhaustion classes. */
#ifdef ECONNREFUSED
        case ECONNREFUSED:  return RUXEN_IO_ERROR_CONNECTION_REFUSED;
#endif
#ifdef ECONNRESET
        case ECONNRESET:    return RUXEN_IO_ERROR_CONNECTION_RESET;
#endif
#ifdef ECONNABORTED
        case ECONNABORTED:  return RUXEN_IO_ERROR_CONNECTION_ABORTED;
#endif
#ifdef ENOTCONN
        case ENOTCONN:      return RUXEN_IO_ERROR_NOT_CONNECTED;
#endif
#ifdef EADDRINUSE
        case EADDRINUSE:    return RUXEN_IO_ERROR_ADDR_IN_USE;
#endif
#ifdef EADDRNOTAVAIL
        case EADDRNOTAVAIL: return RUXEN_IO_ERROR_ADDR_NOT_AVAILABLE;
#endif
#ifdef ETIMEDOUT
        case ETIMEDOUT:     return RUXEN_IO_ERROR_TIMED_OUT;
#endif
#ifdef ENOMEM
        case ENOMEM:        return RUXEN_IO_ERROR_OUT_OF_MEMORY;
#endif
#ifdef ENOSYS
        case ENOSYS:        return RUXEN_IO_ERROR_UNSUPPORTED;
#endif
        /* On Linux EOPNOTSUPP == ENOTSUP, so we guard the second
         * label to avoid a duplicate-case compile error. macOS keeps
         * them distinct. */
#ifdef ENOTSUP
        case ENOTSUP:       return RUXEN_IO_ERROR_UNSUPPORTED;
#endif
#if defined(EOPNOTSUPP) && (!defined(ENOTSUP) || EOPNOTSUPP != ENOTSUP)
        case EOPNOTSUPP:    return RUXEN_IO_ERROR_UNSUPPORTED;
#endif
        default:            return RUXEN_IO_ERROR_OTHER;
    }
}

/* Build a Result::Err(IoError) from a user-supplied message. The
 * resulting variant is always `Other(message)`. Call sites without a
 * meaningful errno (EOF, env-var-not-found, …) use this helper. */
void *ruxen_io_error_message(const char *message) {
    return ruxen_result_err_value((int64_t)ruxen_io_error_other(message));
}

/* Returns true when the tag is one of the 11 message-carrying variants
 * added in #06.5 T1. Lets `from_errno` route the strerror payload into
 * the variant value rather than collapsing back to `Other`. */
static int ruxen_io_error_tag_has_message(int32_t tag) {
    switch (tag) {
        case RUXEN_IO_ERROR_CONNECTION_REFUSED:
        case RUXEN_IO_ERROR_CONNECTION_RESET:
        case RUXEN_IO_ERROR_CONNECTION_ABORTED:
        case RUXEN_IO_ERROR_NOT_CONNECTED:
        case RUXEN_IO_ERROR_ADDR_IN_USE:
        case RUXEN_IO_ERROR_ADDR_NOT_AVAILABLE:
        case RUXEN_IO_ERROR_INVALID_DATA:
        case RUXEN_IO_ERROR_TIMED_OUT:
        case RUXEN_IO_ERROR_WRITE_ZERO:
        case RUXEN_IO_ERROR_UNSUPPORTED:
        case RUXEN_IO_ERROR_OUT_OF_MEMORY:
            return 1;
        default:
            return 0;
    }
}

/* Build a Result::Err(IoError) from a captured errno. Maps the errno
 * onto a curated variant when possible; falls back to
 * `Other(strerror(errno))`. Always capture errno into a local before
 * calling — any subsequent libc call may clobber it. */
void *ruxen_io_error_from_errno(int saved_errno) {
    int32_t tag = ruxen_io_error_classify_errno(saved_errno);
    if (tag == RUXEN_IO_ERROR_OTHER) {
        return ruxen_io_error_message(strerror(saved_errno));
    }
    if (ruxen_io_error_tag_has_message(tag)) {
        return ruxen_result_err_value(
            (int64_t)ruxen_io_error_struct(tag, strerror(saved_errno)));
    }
    return ruxen_result_err_value((int64_t)ruxen_io_error_unit(tag));
}

/* `IoError.message() -> String`. Wired in `codegen/runtime.rs`
 * (`"IoError_message" -> "ruxen_io_error_get_message"`). Returns a
 * heap-allocated String pointer (interned static for unit variants;
 * the captured payload for variants that carry one). */
char *ruxen_io_error_get_message(void *err) {
    if (!err) {
        return ruxen_string_from("io error");
    }
    int32_t tag = *(int32_t *)err;
    switch (tag) {
        case RUXEN_IO_ERROR_NOT_FOUND:
            return ruxen_string_from("entity not found");
        case RUXEN_IO_ERROR_PERMISSION_DENIED:
            return ruxen_string_from("permission denied");
        case RUXEN_IO_ERROR_ALREADY_EXISTS:
            return ruxen_string_from("entity already exists");
        case RUXEN_IO_ERROR_INTERRUPTED:
            return ruxen_string_from("operation interrupted");
        case RUXEN_IO_ERROR_WOULD_BLOCK:
            return ruxen_string_from("operation would block");
        case RUXEN_IO_ERROR_INVALID_INPUT:
            return ruxen_string_from("invalid input");
        case RUXEN_IO_ERROR_UNEXPECTED_EOF:
            return ruxen_string_from("unexpected end of file");
        case RUXEN_IO_ERROR_BROKEN_PIPE:
            return ruxen_string_from("broken pipe");
        case RUXEN_IO_ERROR_OTHER:
        case RUXEN_IO_ERROR_CONNECTION_REFUSED:
        case RUXEN_IO_ERROR_CONNECTION_RESET:
        case RUXEN_IO_ERROR_CONNECTION_ABORTED:
        case RUXEN_IO_ERROR_NOT_CONNECTED:
        case RUXEN_IO_ERROR_ADDR_IN_USE:
        case RUXEN_IO_ERROR_ADDR_NOT_AVAILABLE:
        case RUXEN_IO_ERROR_INVALID_DATA:
        case RUXEN_IO_ERROR_TIMED_OUT:
        case RUXEN_IO_ERROR_WRITE_ZERO:
        case RUXEN_IO_ERROR_UNSUPPORTED:
        case RUXEN_IO_ERROR_OUT_OF_MEMORY: {
            char *msg = (char *)((int64_t *)err)[1];
            if (msg) {
                return msg;
            }
            switch (tag) {
                case RUXEN_IO_ERROR_CONNECTION_REFUSED: return ruxen_string_from("connection refused");
                case RUXEN_IO_ERROR_CONNECTION_RESET:   return ruxen_string_from("connection reset");
                case RUXEN_IO_ERROR_CONNECTION_ABORTED: return ruxen_string_from("connection aborted");
                case RUXEN_IO_ERROR_NOT_CONNECTED:      return ruxen_string_from("not connected");
                case RUXEN_IO_ERROR_ADDR_IN_USE:        return ruxen_string_from("address in use");
                case RUXEN_IO_ERROR_ADDR_NOT_AVAILABLE: return ruxen_string_from("address not available");
                case RUXEN_IO_ERROR_INVALID_DATA:       return ruxen_string_from("invalid data");
                case RUXEN_IO_ERROR_TIMED_OUT:          return ruxen_string_from("operation timed out");
                case RUXEN_IO_ERROR_WRITE_ZERO:         return ruxen_string_from("write zero");
                case RUXEN_IO_ERROR_UNSUPPORTED:        return ruxen_string_from("unsupported");
                case RUXEN_IO_ERROR_OUT_OF_MEMORY:      return ruxen_string_from("out of memory");
                default:                                return ruxen_string_from("io error");
            }
        }
        default:
            return ruxen_string_from("io error");
    }
}

/* `IoError.kind() -> IoErrorKind`. Wired in `codegen/runtime.rs`
 * (`"IoError_kind" -> "ruxen_io_error_kind"`). `IoErrorKind` is a
 * 20-unit-variant enum (no payload) whose tag values match IoError
 * 1:1. The codegen treats every enum value as a 16-byte boxed
 * pointer, so we keep the wire format uniform with IoError itself
 * — tag at offset 0, payload slot zeroed. */
void *ruxen_io_error_kind(void *err) {
    int32_t tag = err ? *(int32_t *)err : RUXEN_IO_ERROR_OTHER;
    int64_t *kind = (int64_t *)ruxen_alloc(16);
    *(int32_t *)kind = tag;
    kind[1] = 0;
    return kind;
}

void *ruxen_stream_handle(FILE *stream) {
    FILE **handle = (FILE **)ruxen_alloc(sizeof(FILE *));
    *handle = stream;
    return handle;
}

FILE *ruxen_stream_from_handle(void *handle, FILE *fallback) {
    return handle ? *(FILE **)handle : fallback;
}

void *ruxen_stream_read_line(FILE *stream) {
    size_t cap = 128;
    size_t len = 0;
    char *buf = (char *)malloc(cap);
    int ch;

    if (!buf) {
        ruxen_panic("out of memory");
    }

    while ((ch = fgetc(stream)) != EOF) {
        if (len + 1 >= cap) {
            size_t next_cap = cap * 2;
            char *next = (char *)realloc(buf, next_cap);
            if (!next) {
                free(buf);
                ruxen_panic("out of memory");
            }
            buf = next;
            cap = next_cap;
        }
        buf[len++] = (char)ch;
        if (ch == '\n') {
            break;
        }
    }

    if (ferror(stream)) {
        int saved_errno = errno;
        free(buf);
        clearerr(stream);
        return ruxen_io_error_from_errno(saved_errno);
    }

    if (len == 0 && ch == EOF) {
        free(buf);
        return ruxen_result_err_value(
            (int64_t)ruxen_io_error_unit(RUXEN_IO_ERROR_UNEXPECTED_EOF));
    }

    buf[len] = '\0';
    return ruxen_result_ok_value((int64_t)buf);
}

int ruxen_saved_argc = 0;
char **ruxen_saved_argv = NULL;

void ruxen_free_saved_argv(void) {
    if (!ruxen_saved_argv) {
        return;
    }

    for (int i = 0; i < ruxen_saved_argc; i++) {
        free(ruxen_saved_argv[i]);
    }
    free(ruxen_saved_argv);
    ruxen_saved_argv = NULL;
    ruxen_saved_argc = 0;
}

void *ruxen_stream_read_to_string(FILE *stream) {
    size_t cap = 128;
    size_t len = 0;
    char *buf = (char *)malloc(cap);
    int ch;

    if (!buf) {
        ruxen_panic("out of memory");
    }

    while ((ch = fgetc(stream)) != EOF) {
        if (len + 1 >= cap) {
            size_t next_cap = cap * 2;
            char *next = (char *)realloc(buf, next_cap);
            if (!next) {
                free(buf);
                ruxen_panic("out of memory");
            }
            buf = next;
            cap = next_cap;
        }
        buf[len++] = (char)ch;
    }

    if (ferror(stream)) {
        int saved_errno = errno;
        free(buf);
        clearerr(stream);
        return ruxen_io_error_from_errno(saved_errno);
    }

    buf[len] = '\0';
    return ruxen_result_ok_value((int64_t)buf);
}

