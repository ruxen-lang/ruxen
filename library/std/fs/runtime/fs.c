#include "../../core/runtime/runtime.h"

/* Generated by Phase B of #06.75: carved from runtime.c.
   DO NOT add to this file unless you also update the top-level runtime.c #include order. */

void *ruxen_fs_read_to_string(const char *path) {
    FILE *stream = fopen(path, "rb");
    void *result;

    if (!stream) {
        return ruxen_io_error_from_errno(errno);
    }

    result = ruxen_stream_read_to_string(stream);
    if (fclose(stream) != 0 && *(int32_t *)result == 0) {
        return ruxen_io_error_from_errno(errno);
    }

    return result;
}

void *ruxen_fs_write(const char *path, const char *contents) {
    /* REPL replay-suppression: the actual write happened on the
     * original input; replay just needs the same Ok(()) shape so
     * downstream `.expect!("write")` chains don't trip. */
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    FILE *stream = fopen(path, "wb");
    const char *text = contents ? contents : "";
    size_t len = strlen(text);

    if (!stream) {
        return ruxen_io_error_from_errno(errno);
    }

    if (fwrite(text, 1, len, stream) != len) {
        int saved_errno = errno;
        fclose(stream);
        return ruxen_io_error_from_errno(saved_errno);
    }

    if (fclose(stream) != 0) {
        return ruxen_io_error_from_errno(errno);
    }

    return ruxen_result_ok_value(0);
}

int64_t ruxen_fs_exists(const char *path) {
    if (!path) {
        return 0;
    }
    return access(path, F_OK) == 0 ? 1 : 0;
}

void *ruxen_fs_remove_file(const char *path) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!path) {
        return ruxen_io_error_message("path is null");
    }
    if (unlink(path) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    return ruxen_result_ok_value(0);
}

void *ruxen_fs_create_dir(const char *path) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!path) {
        return ruxen_io_error_message("path is null");
    }
    if (mkdir(path, 0777) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    return ruxen_result_ok_value(0);
}

void *ruxen_fs_create_dir_all(const char *path) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    char *copy;
    size_t len;

    if (!path || !*path) {
        return ruxen_io_error_message("path is null");
    }

    len = strlen(path);
    copy = (char *)malloc(len + 1);
    if (!copy) {
        ruxen_panic("out of memory");
    }
    memcpy(copy, path, len + 1);

    if (len > 1 && copy[len - 1] == '/') {
        copy[len - 1] = '\0';
    }

    for (char *p = copy + 1; *p; p++) {
        if (*p != '/') {
            continue;
        }
        *p = '\0';
        if (mkdir(copy, 0777) != 0 && errno != EEXIST) {
            int saved_errno = errno;
            free(copy);
            return ruxen_io_error_from_errno(saved_errno);
        }
        *p = '/';
    }

    if (mkdir(copy, 0777) != 0 && errno != EEXIST) {
        int saved_errno = errno;
        free(copy);
        return ruxen_io_error_from_errno(saved_errno);
    }

    free(copy);
    return ruxen_result_ok_value(0);
}

void *ruxen_fs_rename(const char *from, const char *to) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!from || !to) {
        return ruxen_io_error_message("path is null");
    }
    if (rename(from, to) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    return ruxen_result_ok_value(0);
}
int64_t ruxen_fs_is_file(const char *path) {
    if (!path) {
        return 0;
    }
    struct stat st;
    if (stat(path, &st) != 0) {
        return 0;
    }
    return S_ISREG(st.st_mode) ? 1 : 0;
}

int64_t ruxen_fs_is_dir(const char *path) {
    if (!path) {
        return 0;
    }
    struct stat st;
    if (stat(path, &st) != 0) {
        return 0;
    }
    return S_ISDIR(st.st_mode) ? 1 : 0;
}

void *ruxen_fs_read_dir(const char *path) {
    if (!path) {
        return ruxen_io_error_message("path is null");
    }
    DIR *dir = opendir(path);
    if (!dir) {
        return ruxen_io_error_from_errno(errno);
    }
    RuxenVec *names = ruxen_vec_new();
    errno = 0;
    struct dirent *entry;
    while ((entry = readdir(dir)) != NULL) {
        const char *n = entry->d_name;
        /* Skip "." and ".." — callers asking for the directory's
           contents almost never want them, and including them
           complicates downstream sorting / filtering. */
        if (n[0] == '.' && (n[1] == '\0' || (n[1] == '.' && n[2] == '\0'))) {
            continue;
        }
        ruxen_vec_push(names, (int64_t)ruxen_string_from(n));
        errno = 0;
    }
    int saved = errno;
    closedir(dir);
    if (saved != 0) {
        /* `_ORIG_FREE` survives the drop_fixtures textual `free(` →
         * `ruxen_test_free(` splice; the asm-label decl at the top of
         * this file maps it to the public `ruxen_vec_free` symbol. */
        ruxen_vec_ORIG_FREE(names);
        return ruxen_io_error_from_errno(saved);
    }
    return ruxen_result_ok_value((int64_t)names);
}
/* ── Phase 2 stdlib (#06): fs::metadata ────────────────────────────────
 *
 * `ruxen_fs_metadata(path) -> Result[Metadata, IoError]` calls `lstat(2)`
 * so symlinks are reported as `Symlink` rather than dereferenced. The
 * returned `Metadata` is a flat heap struct laid out as three int64s:
 *
 *     offset  0:  size          (file size in bytes, signed for parity
 *                                with Ruxen's `Int`; truncated on
 *                                pathological >2^63 sizes)
 *     offset  8:  modified_secs (UNIX timestamp from st_mtime; seconds
 *                                since 1970-01-01 UTC, ignoring sub-
 *                                second precision)
 *     offset 16:  kind          (0=File, 1=Dir, 2=Symlink, 3=Other —
 *                                covers fifos, sockets, block/char
 *                                devices; the v1 surface is just the
 *                                three predicates `is_file` / `is_dir`
 *                                / `is_symlink`)
 *
 * We pack the fields ourselves so the wire format is independent of
 * libc's `struct stat` ABI (which varies across libc versions and
 * platforms). Accessor helpers read the int64 slots directly. Drop is
 * the generic `ruxen_dealloc` (no inner heap to free), but we also
 * expose a `ruxen_metadata_free` alias for symmetry with future
 * accessor wrappers. */
/* RUXEN_METADATA_KIND_* tag values live in runtime.h so io/file.c
 * (which fills a RuxenMetadata via ruxen_file_metadata) can use the
 * same constants. */

void *ruxen_fs_metadata(const char *path) {
    if (!path) {
        return ruxen_io_error_message("path is null");
    }
    struct stat st;
    /* lstat so a symlink reports as Symlink instead of being followed.
     * Callers that want follow-semantics can call `read_to_string` or
     * `is_file` on the path directly, both of which use plain stat. */
    if (lstat(path, &st) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    int64_t *meta = (int64_t *)ruxen_alloc(24);
    meta[0] = (int64_t)st.st_size;
    meta[1] = (int64_t)st.st_mtime;
    int64_t kind;
    if (S_ISREG(st.st_mode)) {
        kind = RUXEN_METADATA_KIND_FILE;
    } else if (S_ISDIR(st.st_mode)) {
        kind = RUXEN_METADATA_KIND_DIR;
    } else if (S_ISLNK(st.st_mode)) {
        kind = RUXEN_METADATA_KIND_SYMLINK;
    } else {
        kind = RUXEN_METADATA_KIND_OTHER;
    }
    meta[2] = kind;
    return ruxen_result_ok_value((int64_t)meta);
}

/* `ruxen_metadata_{len,modified,is_file,is_dir,is_symlink}` ported
 * to pure Ruxen via `layout c` (see `library/std/io/src/lib.rx`'s
 * `class Metadata`). The Ruxen-side field declarations
 * (`size: Int`, `modified_secs: Int`, `kind: Int`) pin to the same
 * 3-int64 wire layout filled by `ruxen_fs_metadata` /
 * `ruxen_file_metadata` above. */

/* Explicit free helper for symmetry with the other built-in Drop
 * surfaces (Formatter, Vec, Hash). The Metadata struct holds no inner
 * heap, so this is just `ruxen_dealloc` with a typed alias — the
 * scope-exit drop pass uses `ruxen_dealloc` directly for Class-typed
 * locals, so this is currently unused by codegen but exposed for FFI
 * symmetry and future expansion. */
void ruxen_metadata_free(void *meta) {
    if (meta) ruxen_dealloc(meta);
}

/* ── Phase 2 stdlib (#06.5 T3): fs completeness ───────────────────────
 *
 * Six free functions filling the v1 sync-fs surface beyond
 * read/write/exists/remove/create_dir(_all)/rename: copy, recursive
 * remove, canonicalize, atomic write, read_link, symlink. All return
 * Result[T, IoError]. NULL inputs surface `IoError.InvalidInput` (the
 * argument was syntactically wrong, not an I/O failure) — matching the
 * T2 File class's `ruxen_file_invalid_input` convention rather than the
 * legacy `ruxen_io_error_message` "Other" path used by earlier fs fns.
 *
 *   `ruxen_fs_copy(src, dst) -> Result[Int, IoError]`
 *     Read/write loop with a 64 KiB stack buffer. POSIX-portable; we
 *     deliberately do NOT use `copy_file_range(2)` (Linux-only) or
 *     `fcopyfile(3)` (macOS-only) — the loop is fast enough for v1 and
 *     keeps both platforms on the same code path. Returns bytes copied
 *     on success.
 *
 *   `ruxen_fs_remove_dir_all(path) -> Result[(), IoError]`
 *     Hand-rolled post-order traversal via opendir/readdir + lstat.
 *     `lstat` (not stat) so symlinks-to-directories are unlinked
 *     instead of being recursed into — matches `nftw(FTW_PHYS)` and
 *     std::fs::remove_dir_all semantics. We avoid `<ftw.h>` entirely
 *     to dodge the `_XOPEN_SOURCE` feature-test dance on Linux.
 *
 *   `ruxen_fs_canonicalize(path) -> Result[String, IoError]`
 *     `realpath(path, NULL)` — POSIX.1-2008, supported on both macOS
 *     and Linux. The returned buffer comes from libc `malloc`, so we
 *     copy it into a Ruxen String and `free()` (not `ruxen_dealloc`)
 *     the original.
 *
 *   `ruxen_fs_write_atomic(path, contents) -> Result[(), IoError]`
 *     Write to "<path>.tmp.<pid>" in the SAME directory as the target,
 *     fsync the data, fsync the containing directory, then rename(2)
 *     over the target. Same-directory placement is mandatory: rename(2)
 *     is only atomic across paths on the same filesystem, and a tmp in
 *     `/tmp` would cross mounts on most systems. On any error we unlink
 *     the temp file before returning Err so partial writes don't leak.
 *
 *   `ruxen_fs_read_link(path) -> Result[String, IoError]`
 *     Growing-buffer `readlink(2)` — start at 256 bytes, double on
 *     truncation. `readlink` returns the number of bytes written and
 *     does NOT null-terminate, so we always over-allocate by 1.
 *
 *   `ruxen_fs_symlink(target, linkpath) -> Result[(), IoError]`
 *     Thin wrapper around `symlink(2)`. Argument order matches the
 *     Ruxen surface (`symlink(target, link)`) which matches libc.
 */

static void *ruxen_fs_invalid_input(void) {
    return ruxen_result_err_value(
        (int64_t)ruxen_io_error_unit(RUXEN_IO_ERROR_INVALID_INPUT));
}

void *ruxen_fs_copy(const char *src, const char *dst) {
    /* Returns Ok(bytes_copied). Under replay we return Ok(0); the
     * caller's original input already produced the on-disk side
     * effect. */
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!src || !dst) return ruxen_fs_invalid_input();
    int in_fd = open(src, O_RDONLY);
    if (in_fd < 0) {
        return ruxen_io_error_from_errno(errno);
    }
    /* 0644 mirrors File.create. The atomic-vs-non-atomic distinction
     * (truncate first, write into it) matches stdlib `fs::copy` — the
     * destination is left empty if the source open succeeded but the
     * write loop fails partway. */
    int out_fd = open(dst, O_WRONLY | O_CREAT | O_TRUNC, 0644);
    if (out_fd < 0) {
        int saved = errno;
        close(in_fd);
        return ruxen_io_error_from_errno(saved);
    }
    char buf[65536];
    int64_t total = 0;
    for (;;) {
        ssize_t n;
        do {
            n = read(in_fd, buf, sizeof(buf));
        } while (n < 0 && errno == EINTR);
        if (n < 0) {
            int saved = errno;
            close(in_fd);
            close(out_fd);
            return ruxen_io_error_from_errno(saved);
        }
        if (n == 0) break;
        ssize_t written = 0;
        while (written < n) {
            ssize_t w;
            do {
                w = write(out_fd, buf + written, (size_t)(n - written));
            } while (w < 0 && errno == EINTR);
            if (w < 0) {
                int saved = errno;
                close(in_fd);
                close(out_fd);
                return ruxen_io_error_from_errno(saved);
            }
            written += w;
        }
        total += (int64_t)n;
    }
    close(in_fd);
    if (close(out_fd) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    return ruxen_result_ok_value(total);
}

/* Recursive helper for remove_dir_all. Returns 0 on success, -1 on
 * failure with errno set. Post-order: empties children before rmdir'ing
 * the directory itself. Uses lstat so symlinks-to-directories are
 * unlinked rather than recursed-into. */
static int ruxen_fs_remove_dir_all_inner(const char *path) {
    struct stat st;
    if (lstat(path, &st) != 0) {
        return -1;
    }
    if (!S_ISDIR(st.st_mode)) {
        /* Symlink-to-dir or a regular file: unlink, don't recurse. */
        return unlink(path);
    }
    DIR *dir = opendir(path);
    if (!dir) return -1;
    struct dirent *entry;
    size_t path_len = strlen(path);
    int rc = 0;
    errno = 0;
    while ((entry = readdir(dir)) != NULL) {
        const char *n = entry->d_name;
        if (n[0] == '.' && (n[1] == '\0' || (n[1] == '.' && n[2] == '\0'))) {
            continue;
        }
        size_t n_len = strlen(n);
        char *child = (char *)malloc(path_len + 1 + n_len + 1);
        if (!child) {
            errno = ENOMEM;
            rc = -1;
            break;
        }
        memcpy(child, path, path_len);
        child[path_len] = '/';
        memcpy(child + path_len + 1, n, n_len + 1);
        if (ruxen_fs_remove_dir_all_inner(child) != 0) {
            int saved = errno;
            free(child);
            errno = saved;
            rc = -1;
            break;
        }
        free(child);
        errno = 0;
    }
    int saved = errno;
    closedir(dir);
    if (rc != 0) {
        errno = saved;
        return -1;
    }
    if (rmdir(path) != 0) {
        return -1;
    }
    return 0;
}

void *ruxen_fs_remove_dir_all(const char *path) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!path) return ruxen_fs_invalid_input();
    if (ruxen_fs_remove_dir_all_inner(path) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    return ruxen_result_ok_value(0);
}

void *ruxen_fs_canonicalize(const char *path) {
    if (!path) return ruxen_fs_invalid_input();
    /* realpath(path, NULL) allocates via libc malloc on POSIX.1-2008
     * (both macOS and Linux). We always copy into a Ruxen String and
     * free the libc buffer separately. */
    char *resolved = realpath(path, NULL);
    if (!resolved) {
        return ruxen_io_error_from_errno(errno);
    }
    void *result = ruxen_result_ok_value((int64_t)ruxen_string_from(resolved));
    free(resolved);
    return result;
}

void *ruxen_fs_write_atomic(const char *path, const char *contents) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!path) return ruxen_fs_invalid_input();
    const char *text = contents ? contents : "";
    size_t text_len = strlen(text);

    /* Build "<path>.tmp.<pid>" in the SAME directory as `path` — rename(2)
     * is only atomic when source and target are on the same filesystem,
     * and same-directory is the simplest guarantee. */
    size_t path_len = strlen(path);
    /* ".tmp." (5) + 20 digits (max int64 width) + NUL = 26. */
    char *tmp = (char *)malloc(path_len + 32);
    if (!tmp) {
        ruxen_panic("out of memory");
    }
    int written = snprintf(tmp, path_len + 32, "%s.tmp.%ld",
                           path, (long)getpid());
    if (written < 0 || (size_t)written >= path_len + 32) {
        free(tmp);
        return ruxen_io_error_from_errno(EINVAL);
    }

    int fd = open(tmp, O_WRONLY | O_CREAT | O_TRUNC, 0644);
    if (fd < 0) {
        int saved = errno;
        free(tmp);
        return ruxen_io_error_from_errno(saved);
    }
    size_t off = 0;
    while (off < text_len) {
        ssize_t w;
        do {
            w = write(fd, text + off, text_len - off);
        } while (w < 0 && errno == EINTR);
        if (w < 0) {
            int saved = errno;
            close(fd);
            unlink(tmp);
            free(tmp);
            return ruxen_io_error_from_errno(saved);
        }
        off += (size_t)w;
    }
    /* fsync the file's data before rename. The directory entry change
     * (rename) plus an fsync on the file is the canonical Unix recipe
     * for "data hits disk before the new name is visible". A directory
     * fsync would also be needed to guarantee the rename itself is
     * durable across crash; we skip that for v1 — atomicity within a
     * single boot is the only guarantee we promise. */
    if (fsync(fd) != 0) {
        /* fsync failure on some FS / pseudo-fs (e.g. /tmp on certain
         * tmpfs configs) returns EINVAL; do not treat as fatal — the
         * fallback is rename without the durability promise. */
        int saved = errno;
        if (saved != EINVAL && saved != ENOTSUP) {
            close(fd);
            unlink(tmp);
            free(tmp);
            return ruxen_io_error_from_errno(saved);
        }
    }
    if (close(fd) != 0) {
        int saved = errno;
        unlink(tmp);
        free(tmp);
        return ruxen_io_error_from_errno(saved);
    }
    if (rename(tmp, path) != 0) {
        int saved = errno;
        unlink(tmp);
        free(tmp);
        return ruxen_io_error_from_errno(saved);
    }
    free(tmp);
    return ruxen_result_ok_value(0);
}

void *ruxen_fs_read_link(const char *path) {
    if (!path) return ruxen_fs_invalid_input();
    /* readlink does not null-terminate, returns count; on truncation
     * we cannot tell whether the buffer was exactly right or short.
     * Grow until the result is strictly less than the buffer size. */
    size_t cap = 256;
    char *buf = (char *)malloc(cap);
    if (!buf) {
        ruxen_panic("out of memory");
    }
    for (;;) {
        ssize_t n = readlink(path, buf, cap - 1);
        if (n < 0) {
            int saved = errno;
            free(buf);
            return ruxen_io_error_from_errno(saved);
        }
        if ((size_t)n < cap - 1) {
            buf[n] = '\0';
            void *result = ruxen_result_ok_value((int64_t)ruxen_string_from(buf));
            free(buf);
            return result;
        }
        /* Possibly truncated — grow and retry. */
        size_t next = cap * 2;
        char *next_buf = (char *)realloc(buf, next);
        if (!next_buf) {
            free(buf);
            ruxen_panic("out of memory");
        }
        buf = next_buf;
        cap = next;
    }
}

void *ruxen_fs_symlink(const char *target, const char *linkpath) {
    if (ruxen_repl_is_replaying) return ruxen_result_ok_value(0);
    if (!target || !linkpath) return ruxen_fs_invalid_input();
    if (symlink(target, linkpath) != 0) {
        return ruxen_io_error_from_errno(errno);
    }
    return ruxen_result_ok_value(0);
}

