def main
  let v = [1, 2, 3, 4, 5, 6]
  let (evens, odds) = v.partition { |n| n % 2 == 0 }
  puts "evens=#{evens.size}"
  puts "odds=#{odds.size}"
  for e in &evens
    puts "e=#{e}"
  end
  for o in &odds
    puts "o=#{o}"
  end
end
