def main
  let mut v: Vec[Int] = Vec.new
  v.push(10)
  v.push(20)
  v.push(30)
  match v.first
    Some(x) -> puts "first=#{x}"
    None    -> puts "empty"
  end
  match v.last
    Some(x) -> puts "last=#{x}"
    None    -> puts "empty"
  end
end
