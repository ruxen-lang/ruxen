def main
  let mut h: HashMap[Int, Int] = HashMap.new
  h.insert(1, 10)
  h.insert(2, 20)
  h.insert(3, 30)
  let ks = h.keys
  let vs = h.values
  puts "keys_len=#{ks.len}"
  puts "values_len=#{vs.len}"
end
