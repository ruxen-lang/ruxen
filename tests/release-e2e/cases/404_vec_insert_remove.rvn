def main
  let mut v: Vec[Int] = Vec.new
  v.push(1)
  v.push(3)
  v.push(4)
  v.insert(1, 2)
  puts "after_insert=#{v.len}"
  for x in &v
    puts "i #{x}"
  end
  let removed = v.remove(0)
  puts "removed=#{removed}"
  puts "after_remove=#{v.len}"
  for x in &v
    puts "r #{x}"
  end
end
