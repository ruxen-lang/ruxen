def main
  var h: Map[Int, Int] = Map.new
  h.insert(1, 10)
  h.insert(2, 20)
  h.insert(3, 30)
  let before = h.size
  h.clear
  let after = h.size
  let empty = h.empty?
  puts "before=#{before}"
  puts "after=#{after}"
  puts "empty=#{empty}"
end
