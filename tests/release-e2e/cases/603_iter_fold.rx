def main
  let v = [1, 2, 3, 4, 5]
  let total = v.iter.fold(0) { |acc, n| acc + n }
  puts "total=#{total}"
  let empty: Array[Int] = Array.new
  let z = empty.iter.fold(100) { |acc, n| acc + n }
  puts "empty=#{z}"
end
