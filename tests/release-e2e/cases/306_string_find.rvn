def main
  let s = "hello world"
  match s.find("world")
    Some(idx) -> puts "found at #{idx}"
    None      -> puts "missing"
  end
  match s.find("zzz")
    Some(idx) -> puts "found at #{idx}"
    None      -> puts "missing"
  end
end
