def main
  let mut v: Vec[Int] = Vec.new
  v.push(1)
  v.push(2)
  v.push(3)
  v.push(4)
  v.push(5)
  v.retain { |x| x > 2 }
  let n = v.len
  puts "len=#{n}"
  for x in &v
    puts "#{x}"
  end
end
