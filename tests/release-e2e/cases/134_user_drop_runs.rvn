class Holder
  tag: Int

  def init(t: Int)
    self.tag = t
  end

  impl Drop
    def drop
      puts "dropped tag=#{self.tag}"
    end
  end
end

def main
  let h = Holder.new(7)
  puts "before"
end
