def main
  var s: Set[Int] = Set.new
  s.insert(1)
  s.insert(2)
  s.insert(3)
  let before = s.len
  s.clear
  let after = s.len
  let empty = s.is_empty
  puts "before=#{before}"
  puts "after=#{after}"
  puts "empty=#{empty}"
end
