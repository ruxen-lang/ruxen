def main
  var s: Set[Int] = Set.new
  s.insert(1)
  s.insert(2)
  s.insert(3)
  let before = s.size
  s.clear
  let after = s.size
  let empty = s.is_empty
  puts "before=#{before}"
  puts "after=#{after}"
  puts "empty=#{empty}"
end
