def main
  var a: Set[Int] = Set.new
  a.insert(1)
  a.insert(2)
  var b: Set[Int] = Set.new
  b.insert(2)
  b.insert(3)
  let u = a.union(&b)
  let n = u.len
  puts "len=#{n}"
  puts "has1=#{u.contains(1)}"
  puts "has2=#{u.contains(2)}"
  puts "has3=#{u.contains(3)}"
end
