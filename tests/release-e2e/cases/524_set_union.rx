def main
  var a: Set[Int] = Set.new
  a.insert(1)
  a.insert(2)
  var b: Set[Int] = Set.new
  b.insert(2)
  b.insert(3)
  let u = a.union(&b)
  let n = u.size
  puts "len=#{n}"
  puts "has1=#{u.include?(1)}"
  puts "has2=#{u.include?(2)}"
  puts "has3=#{u.include?(3)}"
end
