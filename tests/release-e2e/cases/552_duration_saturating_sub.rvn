use std.time.Duration

def main
  let small = Duration.from_secs(1)
  let big = Duration.from_secs(5)
  let diff_op = small - big
  let diff_named = small.sub(big)
  puts "op_ns=#{diff_op.as_nanos()}"
  puts "named_ns=#{diff_named.as_nanos()}"
end
