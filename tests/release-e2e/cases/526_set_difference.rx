def main
  var a: Set[Int] = Set.new
  a.insert(1)
  a.insert(2)
  a.insert(3)
  var b: Set[Int] = Set.new
  b.insert(2)
  let d = a.difference(&b)
  let n = d.size
  puts "len=#{n}"
  puts "has1=#{d.include?(1)}"
  puts "has2=#{d.include?(2)}"
  puts "has3=#{d.include?(3)}"
end
