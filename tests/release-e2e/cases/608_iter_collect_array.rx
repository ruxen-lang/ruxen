def main
  let v = [1, 2, 3, 4, 5]
  let head = v.take(3)
  puts "head_len=#{head.size}"
  let tail = v.drop(2)
  puts "tail_len=#{tail.size}"
  let all = v
  puts "all_len=#{all.size}"
  let empty: Array[Int] = Array.new
  let z = empty
  puts "empty_len=#{z.size}"
end
