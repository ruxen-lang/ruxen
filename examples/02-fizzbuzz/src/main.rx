## Classic FizzBuzz. Demonstrates `for` over a range, `if`/`elsif`/`else`,
## and string interpolation.
def main
  for i in 1..=20
    if i % 15 == 0
      puts("FizzBuzz")
    elsif i % 3 == 0
      puts("Fizz")
    elsif i % 5 == 0
      puts("Buzz")
    else
      puts("#{i}")
    end
  end
end
