use std.test.Tester
use std.test.Runner

def main
  let r = Runner.new("capture_test")
  Runner.set_current(r.handle_addr)

  Tester.describe("Test") do |t: &var Tester|
    t.it("passes") do
      puts "case_ran"
    end
  end

  r.execute
end
