# Minimal user program for the String anchor pin tests. The
# `takes_string` param exercises the `name → Ty` resolution for the
# `String` annotation: after the zero-Rust-stdlib migration `String` is
# a `DefKind::Class` (its method-home), but `resolve_type_expr` must
# still map the `String` annotation to the primitive `Ty::String` head —
# the pin test reads this param's resolved type to prove the invariant.

def takes_string(s: String) -> Int
  0
end

def main() -> Int
  0
end
