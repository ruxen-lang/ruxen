
use std.rand.random_bytes

def main
  match random_bytes(0)
    Ok(buf) ->
      if buf.len == 0
        puts "ok"
      else
        puts "fail len=#{buf.len}"
      end
    Err(_) -> puts "fail err"
  end
end
