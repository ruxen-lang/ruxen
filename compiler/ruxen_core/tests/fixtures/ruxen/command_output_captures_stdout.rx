use std.process.Command

def report(out: Output)
  if out.stdout.include?("xyz")
    puts "stdout_ok"
  else
    puts "stdout_bad"
  end
end

def main
  match Command.new("/bin/echo").arg("xyz").output
    Ok(out) -> report(out)
    Err(_)  -> puts "err"
  end
end
