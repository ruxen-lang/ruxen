def main
  var s: Set[Int] = Set.new
  s.insert(1)
  s.insert(2)
  s.insert(1)
  puts "#{s.size}"
  if s.include?(1)
    puts "has 1"
  end
  if s.include?(3)
    puts "has 3"
  else
    puts "no 3"
  end
end
